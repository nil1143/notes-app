import { Logout } from "@/components/logout";
import React from "react";

const Dashboard = () => {
  return (
    <div>
      Dashboard
      <Logout />
    </div>
  );
};

export default Dashboard;
